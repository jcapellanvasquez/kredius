/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { StatementImportResponse } from '../../models/statement-import-response';

export interface ReverseStatementImport$Params {
  id: number;
}

export function reverseStatementImport(http: HttpClient, rootUrl: string, params: ReverseStatementImport$Params, context?: HttpContext): Observable<StrictHttpResponse<StatementImportResponse>> {
  const rb = new RequestBuilder(rootUrl, reverseStatementImport.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<StatementImportResponse>;
    })
  );
}

reverseStatementImport.PATH = '/api/v1/statement-imports/{id}/reverse';
